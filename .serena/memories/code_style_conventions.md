# Code style and conventions
- Language: TypeScript with React JSX (`.tsx`), `type: module`.
- TS config: `strict: true`, `jsx: react-jsx`, ES2020 target, bundler module resolution.
- Style observed: function components with named exports, double quotes, semicolons, React.StrictMode.
- CSS: global styles in `src/styles/global.css` with class-based styling.
- No explicit lint/format tooling found in `package.json`.
