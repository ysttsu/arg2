# Task completion checklist
- No dedicated test, lint, or format scripts found.
- Recommended validation: `pnpm build` (TypeScript typecheck + Vite build).
- If running the app locally: `pnpm dev`, then verify UI behavior manually in browser.
